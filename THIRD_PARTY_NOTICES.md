# Third-Party Notices

The Python dependency list is declared in `pyproject.toml`.

Primary runtime dependencies include FastAPI, Uvicorn, HTTPX, Cryptography, PyYAML, Redis client libraries and OpenTelemetry packages. Their licenses must be reviewed before each release using the release candidate dependency inventory.

The community UI is handwritten HTML, CSS and JavaScript and does not bundle third-party browser assets.
