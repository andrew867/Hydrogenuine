"""Memory / proof context rehydration — load prior state as context, never
as truth or authority.

Memory is not truth. Proof is not truth. Prior output is not truth. But
prior receipts are valid context.
"""
