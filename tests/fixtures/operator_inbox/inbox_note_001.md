# Operator-provided local note 001

This is a deterministic, operator-provided local evidence note used only for
LEB-4 operator-inbox tests. It is local-only and non-authoritative.

An operator-provided file is not trusted by default. An evidence receipt is not
truth. A source excerpt is not belief.
