/**
 * Seed helper for mock data, kept simple. In this scaffold, mockDb is in-memory.
 * This script exists so the repo has a spot for future seeding or fixtures import.
 */
console.log("Mock seeding not needed, mockDb is in-memory. You're good.");
